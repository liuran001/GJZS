echo "开始处理"
    [[ $Decode = 0 ]] && {
    echo "------------------------------------------------------"
        echo "处理结果为"
        echo $Input | base64
    }
    [[ $Decode = 1 ]] && {
    echo "------------------------------------------------------"
        echo "处理结果为"
        echo $Input | base64 -d
    }
